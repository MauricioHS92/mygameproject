// aqui temos a configuração do banco de dados

const config = {
    username: 'root',
    password: '3240',
    database: 'pi', //o nome do banco que foi criado no mysql
    host: 'localhost',
    dialect: 'mysql' 
}

module.exports = config;